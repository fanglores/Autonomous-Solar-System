#include "camera.h"

Camera::Camera() //: camera(cv::VideoCapture(0))
{}

//cv::Mat
int Camera::getPhoto()
{
	int frame;
	//cv::Mat frame;
	//camera >> frame;
	
	return frame;
}
//errors processing (?)
